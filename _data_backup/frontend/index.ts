const canvas = document.getElementById('gameCanvas') as HTMLCanvasElement;
const ctx = canvas.getContext('2d')!;

// Just a starter — draw something to test it works!
ctx.fillStyle = 'white';
ctx.fillRect(50, 50, 100, 100);
